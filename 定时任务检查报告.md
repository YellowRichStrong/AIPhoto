# 定时任务检查报告

## 检查时间
2025年12月16日

## 检查结果

### ❌ 定时任务状态：未运行

### 原因分析

1. **Node.js 未安装**
   - 系统未检测到 Node.js 环境
   - 定时任务依赖 Node.js 运行时
   - 相关文件：`scripts/scheduler.js`

2. **Crontab 未配置**
   - 系统 crontab 中没有配置相关定时任务
   - 未设置每天 23:00 自动同步

3. **依赖包未安装**
   - `node_modules` 目录不存在
   - 缺少必需的依赖：`node-cron`, `cheerio`

## 解决方案

### 方案一：安装 Node.js 并启动定时服务（推荐）

#### 步骤：

1. **安装 Node.js**
   ```bash
   # 访问 https://nodejs.org/ 下载并安装
   # 或使用 Homebrew
   brew install node
   ```

2. **安装项目依赖**
   ```bash
   cd /Users/macbookpro/Desktop/html_wangxiao/aitoday
   npm install
   ```

3. **测试同步功能**
   ```bash
   npm run test-sync
   ```

4. **启动定时服务**
   ```bash
   # 方式1：直接运行（需保持终端运行）
   npm start
   
   # 方式2：使用 PM2 管理（推荐，后台运行）
   npm install -g pm2
   npm run install-service
   ```

5. **验证服务状态**
   ```bash
   pm2 list
   pm2 logs aitoday-sync
   ```

### 方案二：使用系统 Crontab

如果不想安装 Node.js，可以使用系统 crontab 调用测试脚本：

```bash
# 编辑 crontab
crontab -e

# 添加以下行（每天 23:00 执行）
0 23 * * * cd /Users/macbookpro/Desktop/html_wangxiao/aitoday && ./test-sync.sh >> logs/cron.log 2>&1
```

### 方案三：手动同步

如果暂时不需要自动同步，可以手动执行：

```bash
cd /Users/macbookpro/Desktop/html_wangxiao/aitoday
# 安装依赖后
npm run sync-now
```

## 定时任务配置信息

### 同步时间
- **执行时间**：每天 23:00
- **时区**：Asia/Shanghai

### 数据源
- **文本生成**：https://model.aibase.com/zh/leaderboard/text-generation
- **其他分类**：根据配置自动拉取

### 目标文件
- **JS数据**：`js/model-center.js`
- **HTML标签**：`index.html`

### 备份目录
- **路径**：`backups/`
- **格式**：`model-center-YYYY-MM-DDTHH-MM-SS.js`

## 验证步骤

安装并启动服务后，请执行以下验证：

1. **检查服务状态**
   ```bash
   pm2 status aitoday-sync
   ```

2. **查看实时日志**
   ```bash
   pm2 logs aitoday-sync --lines 50
   ```

3. **查看备份文件**
   ```bash
   ls -lh backups/ | head -10
   ```

4. **查看同步日志**
   ```bash
   tail -f logs/sync-log.txt
   ```

## 推荐操作

**立即执行：**
1. 安装 Node.js（版本 >= 14.0）
2. 运行 `cd /Users/macbookpro/Desktop/html_wangxiao/aitoday && npm install`
3. 测试同步功能：`npm run test-sync`
4. 如果测试成功，安装 PM2：`npm install -g pm2`
5. 启动后台服务：`npm run install-service`
6. 验证服务：`pm2 list`

## 注意事项

⚠️ **重要提示**：
- 定时任务会自动备份原始数据，备份文件保存在 `backups/` 目录
- 首次运行建议手动测试，确认数据同步正常
- PM2 服务会在系统重启后自动恢复（需配置 `pm2 startup`）
- 日志文件会持续增长，建议定期清理

## 联系支持

如有问题，请检查：
1. Node.js 版本是否 >= 14.0
2. 网络连接是否正常
3. 目标网站是否可访问
4. 文件权限是否正确
